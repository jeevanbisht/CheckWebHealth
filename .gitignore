# Dependencies
node_modules/

# Probe output (may contain cookies/tokens — keep out of git)
akamai-probe-results/

# HAR captures contain cookies, auth headers, tokens — never commit
har/
*.har

# OS / editor
.DS_Store
Thumbs.db
*.log
.vscode/
.idea/
